{
  "name": "buildtool",
  "version": "1.0.0",
  "description": "",
  "main": "BnD.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:client": "node build.js buildClient",
    "build:server": "node build.js buildServer",
    "build": "npm-run-all --parallel build:client build:server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "child_process": "^1.0.2",
    "fs-extra": "^11.1.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
